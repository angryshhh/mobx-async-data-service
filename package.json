{
  "name": "mobx-async-data-service",
  "version": "1.0.0",
  "main": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:angryshhh/mobx-async-data-service.git"
  },
  "scripts": {
    "preversion": "yarn build",
    "prepublishOnly": "yarn build",
    "build": "rimraf dist/ && yarn tsc --project tsconfig.production.json"
  },
  "files": [
    "dist/"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "rimraf": "^5.0.1",
    "typescript": "^4"
  },
  "dependencies": {
    "mobx": "^6.9.0"
  }
}
